# GOVS 規格書

**成品**：Generic Outcome-Oriented Verification System
**文件層級**：規格層
**遵循標準**：〈成品規劃與規格形成〉（外部標準，非本專案內容）

---

## 0. 文件定位

### 0.1 本文件回答什麼

> 成品必須是什麼。

技術條件見技術約束書；實際做法見程式碼；證明方式見驗證文件。

### 0.2 本文件刻意不含什麼

依規格與實作分離原則，本文件不出現：程式語言、框架、套件、
函式名稱、類別名稱、檔案名稱、開發工具、特定實作慣例、
特定資料交換格式、特定結構定義語法。

**判準**：若一句話在更換技術之後失去意義，它就不屬於本文件。

### 0.3 成熟度標記

每條規格標示其成熟度。標記語彙沿用本專案所遵循之規格形成標準，
其定義不在本文件重述：

✅ 已定案　⏳ 待確認　🔍 推導中　⚖️ 候選方案
⚠️ 規格外待判定　📌 已知限制　🗑️ 已否決

（「待製作確認」不出現於本文件——見第 7 節開頭。）

本文件的成熟度標記**有實作證據支撐**——標為 ⏳ 或 ⚠️ 者，
都是實際建置過程中撞到的，不是紙上推測。

---

## 1. 成品定義

### 1.1 成品是什麼 ✅

一套接收既定驗收條件與候選成果，判定該成果是否達成條件，
並決定任務應繼續修訂、正常結案或異常終止的閉環系統。

### 1.2 成品要解決什麼 ✅

當一個產出過程結束後，三個問題通常沒有可判定的答案：

1. 這份成果算不算完成
2. 若不算，還差什麼，且差距要能轉成下一步動作
3. 什麼時候該停

### 1.3 如何判定成品達成需求 ✅

成品達成需求，當且僅當：

- 給定一組驗收條件與一份成果，系統能對每一條條件產出可追溯的判定
- 未達成時，能產出定向到具體條件的差距描述，而非要求整份重做
- 修訂迴圈能自行收斂，不依賴外部強制中止
- 無法完全判定時，系統表達該限制而非宣稱成功
- 相同輸入重複執行，得到相同判定與相同狀態軌跡


---

## 2. 責任分層

### 2.1 四層責任 ✅

| 層 | 負責 | **不負責** |
|---|---|---|
| 人類權威 | 提出意圖、界定不可妥協邊界、界定資源上限、最終裁決 | 追蹤微觀收斂狀態、定義低階檢查方式 |
| 意圖轉譯 | 判別意圖是否落在已知範圍、未知時停止並交還決策權、將人類指認的案例轉為可驗證條件 | 產生成果內容、判定成果是否達標 |
| 驗證核心 | 對照條件驗證成果、產生差距、判定終止、封裝結論 | 理解人類意圖、產生領域內容、發明條件 |
| 執行 | 產生候選成果、依差距進行局部重構 | 驗證自身產出、決定任務終止 |

### 2.2 分層的必要條件 ✅

- **驗證與生成必須拓撲分離。** 生成方不得驗證自身產出。
- **意圖轉譯的失敗必須向上，不得向下。** 無法轉譯時停止並交還人類，
  不得以任何形式推測後放行。
- **驗證核心不得因規格缺口而自行補值。** 遇未定義事項應停止並回報。

### 2.3 上下游契約 ✅

驗證核心的**輸入**必須完整就緒且由外部提供：

- 驗收條件集合，含條件之間的依賴關係
- 資源上限
- 每條條件對應的檢查方式

驗證核心的**輸出**：

- 結論（含無法完全判定時的表達）
- 差距描述（未達成時）
- 逐輪紀錄

---

## 3. 必須具備

### 3.1 驗收條件的分類 ✅

驗收條件必須分為三類分別處理，不得合併為單一分數：

| 類別 | 值域 | 行為 |
|---|---|---|
| 不可妥協條件 | 二值 | 任一不成立即禁止結案 |
| 結構條件 | 二值 | 獨立的阻擋條件 |
| 品質目標 | 連續 | 有滿意門檻，有停止改善的死區 |

**必要理由**：合併為單一加權分數會使「大量品質提升抵銷不可妥協條件的失敗」
成為可能。一份不可妥協條件缺一項但品質分數很高的成果，
會勝過條件全數通過但品質普通的成果。

### 3.2 條件之間的依賴 ✅

- 條件集合必須構成有向無環結構
- 上游條件未通過時，下游條件標記為未執行，而非判為失敗
- 差距描述只包含根本原因，不包含因依賴而未執行的項目

**必要理由**：若不區分，單一根因會衍生大量假失敗，定向修復隨即失去意義。

### 3.2.1 未執行與未通過的區分必須貫穿到底 ✅

「標記為未執行」不足以構成區分。該區分必須在**所有下游環節**維持，
包含聚合、彙總、對外結論與任何以數值表達的總計。

**不得**在任一環節將未執行併入未通過，即使兩者導致相同的後續動作。

| 檢查是否發生 | 檢查的結果 |
|---|---|
| **已執行** | 通過 / 未通過 |
| **未執行** | 無結果可言 |

未執行時**沒有結果**，而不是「結果為空」或「結果為不通過」。
兩者的差別在於：結果為空仍是一個可被讀取、可被比較的值，
沒有結果則取不到值。

**未執行者必須記錄其成因**——是哪一條上游條件未通過而導致。
無法指出成因的「未執行」等同於靜默略過（N-14）。

**必要理由**：紀錄層區分而聚合層不區分，等於區分沒有發生。
下游讀到的是聚合後的數值，而該數值若把「沒去檢查」與「檢查了不合格」
壓成同一個值，區分就在傳遞過程中消失了——而且消失得無聲無息，
因為紀錄層看起來完全合規。

**已知後果**：本區分若貫穿到底，「全部通過」這類總計值在
存在未執行項時將無法成立，必須改以「已執行幾項、其中通過幾項、
未執行幾項」表達。單一數值承載不了三個事實。

### 3.3 每輪必須全量重新驗證 ✅

每一輪修訂之後，必須重新檢查全部條件，不得只檢查上一輪失敗的部分。

**必要理由**：只檢查修改處，就無法發現「修好 A 卻弄壞 B」。
而異常控制（3.7）依賴看得見這種回歸，兩者是同一個決定的兩面。

### 3.4 局部修復與凍結 ✅

- 未達成時產生**定向**的差距描述，指出具體條件與目標位置
- 已通過的部分標記為凍結，執行層不得主動修改
- **凍結約束生成，不約束驗證**

**必要理由**：見 3.3。

### 3.5 生命週期 ✅

系統必須以明確狀態承載任務的生命週期，且每次狀態轉移都有可追溯的觸發原因。

必須能區分的狀態至少包含：待命、載入、執行、評估、修訂、終止評估、
正常結案、人類仲裁、異常終止。

### 3.6 終止判定 ✅

正常結案的必要條件：

1. 全部不可妥協條件成立
2. 全部結構條件成立
3. 整體品質達到滿意門檻
4. 本輪的邊際改善落入死區，或單位資源的邊際回報低於門檻

**首輪不得結案。**

### 3.7 異常控制 ✅

必須偵測並處置以下兩種無法自行脫離的情況：

| 情況 | 必須的處置 |
|---|---|
| 修好一項就弄壞另一項，反覆交替 | 回滾至歷史最佳版本，交人類仲裁 |
| 連續數輪無實質進展且不可妥協條件仍未通過 | 由局部修訂切換為全域重構；仍無進展則異常終止 |

另須有資源上限與迭代上限作為兜底。

### 3.8 結論的表達 ✅

結論**不得**是布林值或單一分數。必須能表達下列彼此不同的狀態：

- 全部判定且通過
- 已結束但仍有未判定或未達標項目
- 結束時仍有阻擋條件未解除
- 系統無法在無人介入下作出結論

且必須附帶：阻擋原因、未完成原因、不確定因素、人類介入需求。

### 3.9 判定的成立條件與適用邊界 ✅

每份結論必須標明：

- 該判定是如何形成的（依據哪些觀測）
- 該判定在什麼範圍內成立
- 該判定屬於單輪具體檢核，還是跨輪的整體驗收

**這三項由上游提供，驗證核心不推導、不生成、不填補。**
上游未提供時，系統不得自行編造，應回報無法產出合規結論。

### 3.10 證據分級 ✅

證據的成立必須**依序**通過三個條件，不得跳過或錯置：

```
可客觀觀測  →  有明確定義  →  通過經驗類比
```

由此形成兩層：

| 層 | 通過到第幾項 |
|---|---|
| 可參考 | 第一、二項 |
| 完備 | 三項全數通過 |

**順序不可逆。** 先取得觀測，才談得上為它形成定義；
定義未成立之前，沒有可供比對的對象，類比無從進行。

**必要理由**：順序若被視為可交換的並列條件，
就會出現「先寫好定義，再去找符合定義的觀測」這種倒置。
那是把既有框架套上去，不是形成理解。3.14 要求先索取案例
而非先索取規格細節，依據即在此。

**類比失敗不得使觀測消失。** 首次出現、找不到類比對象的現象，
其觀測仍然成立，只是停在可參考層。

### 3.11 判準自身的證據等級 ✅

驗收條件本身也有證據等級，適用同一套分級。

判準由人類指認的可參考案例推導而來：

- 單一案例：可觀測、可定義成立，但無既有經驗可轉移 → **可參考**
- 同一判準在第二份獨立案例中仍然成立 → **完備**

**必須維持的關係**：見 5.1。

### 3.12 信任階層 ✅

多來源的條件在衝突時，依固定優先序處置：

```
人類顯式指令 ≻ 領域不變量 ≻ 客觀上下文真值 ≻ 推論假設
```

- 低階層與高階層衝突時，低階層的檢驗無效化，且不計入整體判定
- 人類顯式指令與領域不變量直接衝突時，**系統不自行裁決**，交人類仲裁

### 3.13 意圖的規格內外判別 ✅

- 意圖落在已知範圍內：放行
- 存在未知維度：**在任何下游動作發生之前**停止，並交還決策權
- 停止時必須明確回報是哪些維度未知

### 3.14 未知意圖的轉譯 ✅

處理未知維度的程序：

1. 索取人類指認的可參考案例，而非追問規格細節
2. 從案例逆向解構出可量化的觀測錨點
3. 將錨點交人類確認，可接受、拒絕、微調、降級
4. 人類確認後才寫入條件字典

**第 3 步是唯一的攔截點**：逆向解構的結果在人類確認之前不得寫入。

### 3.15 條件字典的演化 ✅

| 紀律 | 內容 |
|---|---|
| 作用域隔離 | 條件定義綁定其適用範圍；未顯式發布為全域則不跨範圍 |
| 版本遞增 | 調整數值或追加非阻擋條件屬小幅演化 |
| 強制分支 | 變動阻擋型條件時封存舊版，確保過往驗收可重複溯源 |
| 不可弱化 | 具最高否決權的合規條款，任何衍生版本不得移除或弱化 |

### 3.16 可重現性 ✅

相同輸入必須產出相同的判定、相同的狀態軌跡、相同的結論。

**必要理由**：終止判定、資源判定與異常偵測若不可重現，即無法被驗證。

### 3.17 檢查方式本身的成立條件 ✅

證據分級（3.10）對**被檢查的對象**設下條件。
檢查方式**自身**也有條件，兩者不可混為一談。

一個檢查方式若要能產出完備層證據，必須具備：

| 條件 | 內容 |
|---|---|
| 狀態維持 | 能保存自身狀態，不是每次從零開始 |
| 交互歷史 | 能取用先前輪次的紀錄 |
| 更新能力 | 先前的經驗能改變後續判斷 |

三者缺一，即只存在「輸入 → 處理 → 輸出」，
而不存在「經驗 → 改變 → 新判斷」。

**此類檢查方式的判定只能停在可參考層，不得升至完備層。**
且此一限制必須被明確標示，不得靜默略過（見 N-14）。

### 本條不規定檢查方式該如何建置

檢查方式由外部提供（見 2.3）。**它如何產生、由誰決定、依什麼順序做出來，
皆不在本規格範圍內。**

本條規定的只有一件事：**該檢查方式的結果能宣稱到哪一層。**

```
怎麼做出來的     使用者的事
能宣稱到哪一層   本系統的事
```

因此本條不要求任何人建置具備狀態的檢查方式，
也不預設任何開發順序。一個每次從零開始的固定規則完全合法——
它只是不能宣稱自己完成了經驗類比。

**必要理由**：完備層的成立條件是通過經驗類比。
沒有交互歷史就沒有類比對象，因此無狀態的檢查方式在結構上
不可能形成類比——但它仍然吐得出一個看起來正常的分數。
不設此條，系統會在「根本沒有能力比對」的情況下宣稱比對通過。

**已知後果**：本系統目前採用的檢查方式，多數為固定規則，
不具備上述三項。這不是缺陷——固定規則正是可重現性（3.16）的來源——
但意味著**這類判定天生停在可參考層**，該事實必須出現在結論中。

---

## 4. 不得發生

這一節是本規格最不可讓步的部分。

| # | 不得發生 | 成熟度 |
|---|---|---|
| N-1 | 以品質分數抵銷不可妥協條件的失敗 | ✅ |
| N-2 | 在無法完全判定時宣稱成功 | ✅ |
| N-3 | 生成方驗證自身產出 | ✅ |
| N-4 | 遇到未知維度時以推測方式放行 | ✅ |
| N-5 | 將未經檢查的主張當作證據 | ✅ |
| N-6 | 以系統自身產物，佐證產生該產物所依據的判準 | ✅ |
| N-7 | 在規格未定義處填入預設值後繼續執行 | ✅ |
| N-8 | 判定退化為單一分數或布林值 | ✅ |
| N-9 | 宣稱超過判準本身可信度的結論 | ✅ |
| N-10 | 以模糊描述解釋模糊描述 | ✅ |
| N-11 | 只驗證修改處 | ✅ |
| N-12 | 讓無法被檢查的條款進入驗收條件集合 | ✅ |
| N-13 | 未經人類確認即將逆向解構結果寫入條件字典 | ✅ |
| N-14 | 因無法判定而靜默略過某條件 | ✅ |
| N-15 | 以不具交互歷史的檢查方式產出完備層證據 | ✅ |

### N-6 的理由 ✅

系統產物是被某條判準量出來的。拿它回頭佐證同一條判準，
等於用尺量出來的結果去證明尺是準的。

佐證只能來自**獨立於本系統的人類案例**。

### N-9 的理由 ✅

見 5.1。

---

## 5. 必須維持的關係

### 5.1 結論的可信度不得超過判準的可信度 ✅

> 驗證結論不可能比它所依據的判準更牢靠。

即使本輪觀測的類比一致性很高，若判準本身只建立在單一案例上，
結論仍不得宣稱完備——尺規本身還是暫定的。

被此規則擋下的項目必須被列出，不得靜默消失。

### 5.2 凍結範圍與回歸偵測的關係 ✅

凍結約束生成，不約束驗證。兩者不是獨立機制，是同一決定的兩面。

若凍結同時約束驗證，回歸偵測與異常控制（3.7）將一併失效。

### 5.3 資源消耗與品質改善的關係 ✅

終止判定需用到一個同時牽涉品質變化與資源消耗的量。

**該量的計量方式不屬於規格層**——資源消耗的單位在製作之前無法判定，
其定義記錄於技術約束層。

### 5.4 上游條件分類與驗證分類的關係 ✅

上游依「必要性與極性」分類，驗證核心依「驗證性質」分類。
兩者都是三分，**切分軸不同**，因此不存在自然對應。

缺的資訊是**每條要求對結案的關聯強度**——上游只分得出「必須」與「加分」，
下游需要三種關聯。中間那一格（必須，但可打折）從未被詢問。

**取得方式**：於人類確認錨點時一併詢問——
「這一條缺了就不能交件，還是可以打折？」
該問題只有人答得出來，因為它問的是重要程度，不是產物的物理特徵。

**在該資訊未取得時，採固定映射**：

| 上游 | 驗證核心 |
|---|---|
| 必須具備 | 不可妥協條件 |
| 絕對不可具備 | 不可妥協條件，條件反向（該特徵的出現次數必須為零） |
| 加分項 | 品質目標 |

**固定映射不會產生錯誤判定，只會過嚴**——所有「必須具備」皆視為一票否決。
其代價是無法表達「必須，但可打折」。

**結構條件不由上游產生。** 它是「能否開始檢查」的前提，
屬驗證核心自身的判斷，不在映射範圍內。

**必要理由**：兩軸正交而強行對應，會使負向要求無處可放、
加分項的權重無來源、通過條件與不可妥協條件語意衝突。
固定映射以「過嚴」換取「每個判定都有依據」，
而過嚴是可被察覺的，錯誤的映射不是。

### 5.5 差距描述與原始意圖的關係 ✅

差距描述中的修復提示必須可追溯回原始的抽象意圖，
而非只給出條件識別碼。

### 5.6 最佳版本的比較方式與 N-1 的關係 ✅

「回滾至歷史最佳版本」與「輸出最佳版本」需要一個比較方式。

**該比較方式必須是分層的，不得是加權總分。**

必要理由：加權總分會使 N-1 所禁止的抵銷重新出現——
一份不可妥協條件缺一項但品質極高的成果，會勝過條件全過但品質普通的成果。
分層比較則使低層永遠無法翻轉高層。

**比較鍵的實際層序不屬規格層**，其定義記錄於技術約束層。

---

## 6. 如何判斷是否符合要求

以下是判斷各條規格是否被遵守的**判準**——看什麼就能斷定符合與否。
不是測試程序；測試怎麼跑屬驗證層。

| 規格 | 符合的判準 |
|---|---|
| 3.1 條件分類 | 三類條件各自獨立參與判定，且不存在把三類合併為單一數值的路徑 |
| 3.2 依賴 | 上游未通過時，下游的狀態是「未執行」而非「未通過」；差距描述中不含未執行項 |
| 3.2.1 區分貫穿 | 任一總計值中，未執行項未被計入「未通過」；且每個未執行項都指得出成因 |
| 3.3 全量重驗 | 任一輪的檢查涵蓋全部條件，與該條件上一輪的結果無關 |
| 3.4 凍結 | 下發的修訂指令帶有凍結範圍，且該範圍等於本輪已通過的部分 |
| 3.5 生命週期 | 每次狀態轉移都留有觸發原因，且轉移序列可完整重建 |
| 3.6 終止判定 | 正常結案時，四項必要條件同時成立；任一不成立即未結案 |
| 3.7 異常控制 | 兩種無法自行脫離的情況各有其處置，且處置後不再回到原迴圈 |
| 3.8 結論表達 | 結論可落在四種狀態之一，且四段說明皆有內容或明確為空 |
| 3.9 成立條件與邊界 | 三項標明皆來自外部輸入；外部未提供時系統拒絕產出結論 |
| 3.10 證據分級 | 類比失敗的項目仍保有其觀測，且停在可參考層而非消失；三項條件的檢查順序不可交換 |
| 3.11 判準證據等級 | 單案例判準為可參考；第二份獨立案例佐證後方為完備 |
| 3.12 信任階層 | 低階層與高階層衝突時低階層失效；最高兩層衝突時系統不自行裁決 |
| 3.13 規格內外判別 | 存在未知維度時，任何下游動作皆未發生 |
| 3.14 未知意圖轉譯 | 人類確認之前，條件字典中不存在該定義 |
| 3.15 條件字典演化 | 阻擋型條件變動時舊版被封存；合規條款的移除被拒絕 |
| 3.16 可重現性 | 相同輸入的兩次執行，判定、軌跡、結論三者完全一致 |
| 3.17 檢查方式的成立條件 | 不具交互歷史的檢查方式，其判定停在可參考層，且該限制出現在結論中而非靜默略過 |
| 4 不得發生 | 十五項各自不存在對應的發生路徑 |
| 5.1 可信度上限 | 判準為暫定時，結論不宣稱完備，且被擋下的項目被列出 |
| 5.2 凍結與回歸 | 凍結存在的同時，回歸仍可被偵測 |
| 5.4 上下游映射 | 未取得關聯強度時採固定映射；映射結果過嚴而非錯誤；結構條件不來自上游 |
| 5.5 差距與意圖 | 修復提示含原始意圖的可讀描述，而非僅有識別碼 |
| 5.6 最佳版本比較 | 比較方式為分層，且低層無法翻轉高層 |

**判準與規格條文一對一。** 若某條規格找不到判準，該條規格應重新檢查
它是否仍屬可執行規格。

---

## 7. 哪些內容目前尚未定義

> **本節只收錄「製作開始之前就能判定、但目前尚未判定」的事項。**
>
> 任何必須進入製作才能確定的內容，不屬於規格層——
> 因此本節不會出現「待製作確認」這個成熟度。有的話，代表那項不該在這裡。


### 7.3 判準之間的語意衝突偵測 📌

信任階層（3.12）的處置以「衝突已被偵測到」為前提，
但判斷兩條判準是否語意衝突，需要語意理解能力。

**已知限制**：本系統只處理**顯式宣告**的衝突。
自動偵測語意衝突屬上游能力，不在驗證核心範圍。

### 7.4 缺少品質目標的條件集合 ⚠️

正常結案的必要條件包含「整體品質達到滿意門檻」。
若一組驗收條件完全不含品質目標，該條件永遠不成立。

**已知後果**：只有不可妥協條件與結構條件的任務——例如純格式校驗、
純合規檢查——即使全數通過也無法正常結案。

**發現方式**：製作階段。紙上看不出來，因為紙上的例子都同時含三類條件。

### 7.5 條件無法被判定時的阻擋地位 ⚠️

阻擋條件的定義只涵蓋不可妥協條件與結構條件。
當某個品質目標**無法被判定**時（不是未通過，是判不出來），
該條件被排除於品質計算之外；若其餘條件仍讓整體品質達標，任務會照常結案。

**已知後果**：結論中明確有一項「無法判定」，任務狀態卻是完成。

**這是定義問題不是運作問題**：阻擋條件的涵蓋範圍在製作之前就該定清楚。

---

### 7.6 逆向解構的能力來源 📌

未知意圖的轉譯（3.14）第 2 步需要語意理解能力。

**已知限制**：這是整套系統中唯一無法以確定性邏輯實作的步驟。
其能力由外部提供，但**其輸出契約由本系統強制檢查**：
違反 N-10、無法量化、或提不出精確判準者，一律拒收。

### 7.7 凍結範圍無法由系統強制執行 📌

規格 3.4 要求已通過的部分標記為凍結。但成果的子元件分解方式未定義，
系統無從驗證執行層是否真的沒有改動凍結範圍。

**已知限制**：系統負責正確計算並下發凍結範圍，強制執行屬執行層責任。
緩解方式為全量重驗（3.3）——違反凍結所造成的回歸仍會被抓到，
只是抓到的時機是事後而非事前阻止。

---

### 7.8 下一輪改善量的預估 🗑️

早期文件的執行追蹤範例中，終止判定使用了「預估下一輪邊際增益」。

**已否決**：終止數學中不存在此元件。終止判定只使用已觀測的量。

---

## 8. 成熟度總表

| 節 | 內容 | 成熟度 |
|---|---|---|
| 1 | 成品定義 | ✅ |
| 2 | 責任分層 | ✅ |
| 3.1–3.5 | 條件分類、依賴（含 3.2.1 區分貫穿）、全量重驗、凍結、生命週期 | ✅ |
| 3.6 | 終止判定 | ✅ |
| 3.7 | 異常控制 | ✅ |
| 3.8–3.17 | 結論、證據、信任、轉譯、演化、可重現、檢查方式自身 | ✅ |
| 4 | 不得發生（15 項） | ✅ |
| 5.1、5.2、5.5、5.6 | 可信度上限、凍結關係、可追溯、最佳版本比較 | ✅ |
| 5.3 | 資源與品質的關係 | ✅ |
| 5.4 | 上下游分類映射 | ✅ |

| 7.4、7.5 | 無品質目標、無法判定的阻擋地位 | ⚠️ |
| 7.3、7.6、7.7 | 語意衝突、逆向解構、凍結強制 | 📌 |
| 7.8 | 下一輪預估 | 🗑️ |

### 8.1 統計

| 成熟度 | 項數 |
|---|---|
| ✅ 已定案 | 大多數 |
| ⏳ 待確認 | **0** |
| ⚠️ 規格外待判定 | 2（7.4、7.5）|
| 🔨 待製作確認 | **0**（規格層不得有此類） |
| 📌 已知限制 | 3 |
| 🗑️ 已否決 | 1 |

### 8.2 阻斷性判讀

- **目前無阻斷性缺口。** 上下游映射（原 7.2）已於 5.4 定案：
  取得關聯強度前採固定映射，其結果過嚴而非錯誤。
- 其餘 ⚠️ 皆已有暫行處置，系統可運行，
  但暫行處置屬技術約束層，不得視為規格。
- 標為 📌 者已判定為可接受，不列入待辦。
- 標為 🗑️ 者不得重新引入。
